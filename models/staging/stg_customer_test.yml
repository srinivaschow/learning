version: 2

models:
  - name: customers.sql
    columns:
      - name: customerid
        tests:
          - unique
          - not null
      - name: segment
        tests:
          - accepted_values:
              values:
              - consumers
              - home office
              - corporate
                
              
     
      

